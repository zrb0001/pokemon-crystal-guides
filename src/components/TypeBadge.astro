---
import { typeZh } from '../lib/data';

interface Props {
  type: string;
}
const { type } = Astro.props;
---

<span class={`type-badge type-${type}`}>{type === 'unknown' ? '???' : typeZh(type)}</span>
